# PROJECT_SPEC — JLAB TECH Business AX MVP

## 1. 프로젝트 목적

제이랩테크가 **"계측기를 납품하고 끝나는 회사"** 에서
**"고객이 구매한 장비의 운영을 계속 함께 관리하는 회사"** 로 확장하는 과정을,
실제로 작동하는 화면과 기록으로 보여주는 통합 AX MVP.

단순 내부 관리 프로그램이 아니라 다음 세 가지를 동시에 만족해야 한다.

1. 대표자가 보고 "우리 회사에 실제로 필요하다"고 느끼는 실사용 수준의 데모
2. 고객사가 직접 쓰는 B2B 플랫폼과, 그 요청이 내부로 이어지는 Closed Loop
3. 실제 사용 기록이 쌓여 사업고도화·정책자금·R&D 심사의 근거가 되는 구조

## 2. 대상 회사

- 회사명: 제이랩테크(주) / JLAB TECH
- 업종: 산업·환경 계측기, 센서, 분석 장비 공급
- 참고 규모: 2025년 매출 약 4.62억원, 재고자산 약 2.25억원
- Demo 데이터는 이 규모에 맞춰 구성한다. 수십억 단위 수치는 쓰지 않는다.

## 3. 두 개의 Surface

### Business AX (내부 운영)

| 그룹 | 화면 |
| --- | --- |
| 운영 | 대시보드 · 고객 요청 · 재고·수요 관리 · AI 추천·견적 · 재구매 예측 |
| 수익·자산 관리 | 마진 가드 · 설치장비 관리 · 산업계측 설계 |
| 분석·성과 | AX 실증성과 · 리포트 센터 · 정책자금 성과 분석 |
| 안내 | 기획의도 · 대표 시연 · 사용 방법 · 설정 |

### Customer Platform (고객용)

| 경로 | 화면 |
| --- | --- |
| `/customer` | 고객 홈 — 요약, 다가오는 일정, 빠른 요청, 최근 요청, 관리 중 장비 |
| `/customer/equipment` | 내 장비 — 검색·필터, 교정 시점·보증, 운영 이력, 장비별 요청 |
| `/customer/requests` | 요청 내역 — 진행 단계와 제이랩테크 답변 |
| `/customer/services` | 서비스 & 확장 로드맵 — 이용 가능 / 준비 중 / 검토 중 |

쇼핑몰이나 랜딩 페이지가 아니다. **고객 장비 운영지원 포털**이다.

## 3-1. 서비스 확장 로드맵 표기 원칙

`/customer/services`는 "지금 되는 것"과 "앞으로 할 것"을 같은 카드 구조로 놓는다.

| 단계 | 의미 |
| --- | --- |
| 이용 가능 | 이 플랫폼에서 지금 동작한다 |
| 준비 중 | 필요한 데이터는 이미 있고 기능 구현만 남았다 |
| 검토 중 | 실증·인증이 선행되어야 한다. 동작하지 않는다 |

각 항목은 [지금 준비된 것 / 어떻게 구현되는가 / 고객이 얻는 것 / 수익 구조 /
선행 조건] 다섯 칸을 모두 채운다. **예상 매출액은 쓰지 않는다.** 수익은 금액이 아니라
구조(반복 매출 / 건별 매출 / 자산 활용 / 유지·락인)로만 적고, 적용 대상 규모는
지어내지 않고 등록된 장비 수에서 센다. 선행 조건은 반드시 함께 적는다.

## 4. Closed Loop (핵심)

```
고객 플랫폼            Business AX              직원 처리            고객 플랫폼
장비 확인      →      요청 접수        →      검토·제안·처리   →   상태 업데이트
교정·소모품 요청       재고·견적 판단           발주/견적 생성        완료 안내
                                    ↓
                    요청 데이터 축적 → 재구매 예측 → 재고 수요 → 다음 제안
```

구현된 루프 2개:

- **LOOP A 교정 요청** — 내 장비에서 교정 요청 → `/requests` 노출 → 상태 전진 → 고객 화면 반영
- **LOOP B 소모품·추가 계측** — 동일 경로, 견적과 연결 가능(`quoteId`)

## 5. 1·2·3단계 AX

| 단계 | 성격 | 범위 | 상태 |
| --- | --- | --- | --- |
| 1단계 | 효율화형 | 재고·발주·견적·재구매·마진·교정 | 운영 중 |
| 2단계 | 사업모델 전환형 | 고객 플랫폼, 장비 관리, 교정·소모품 요청, 계측 설계 | 시작 단계 |
| 3단계 | 산업전환형 | 원격 계측, 데이터 리포트, 이상징후, 예지보전, 정기관리 계약 | **중장기 검토** |

3단계는 구현된 기능이 아니다. 화면에서도 "3단계 검토" 배지로만 노출한다.

## 6. 기술 스택

- Next.js 14 (App Router) + TypeScript
- Tailwind CSS — 색상은 전부 CSS 변수(`globals.css`의 `[data-theme]` 블록)를 가리킨다
- Framer Motion (등장·전환), Recharts (차트), Lucide (아이콘)
- 상태: mock data(읽기) + localStorage(쓰기)

```
읽기: 화면 → data/repository.ts → data/mock/*        → 이후 Supabase select
쓰기: 화면 → lib/store-context.tsx → localStorage    → 이후 Supabase insert/update
```

## 7. 디자인 원칙

- **Ivory & Deep Green** 이 기본 브랜드(Forest Sand 테마). 다른 테마는 시연용 선택지일 뿐 기본이 아니다.
- 보조 강조색은 **clay(따뜻한 테라코타)** 와 **mist(차가운 슬레이트블루)** 두 가지뿐이다.
  둘 다 CSS 변수로 테마 6종에 정의되며, 쓰는 곳을 제한한다 — 준비 중(clay)·검토 중(mist),
  소모품 일정, 알림 배지. 색을 더 늘리지 않는다.
- Canvas는 아이보리, 업무 카드는 **순백(White)** — 데이터 표면의 대비를 확보한다.
- Sidebar는 딥그린, 아이콘은 저채도 5색으로 구분.
- 모션은 장식이 아니라 반응성: Hover/Button 160~180ms, Drawer 180~220ms. 무한 반복 애니메이션 금지.
- 텍스트 overflow 금지. `clamp-1/2/3`, `truncate`, 고정 높이, `min-w-0` 을 기본으로 쓴다.
- 글자 크기 3단계(작게/기본/크게)가 실제로 동작하고 localStorage에 저장된다.
- **고객 화면은 업무 화면보다 한 단계 큰 글자**를 쓴다. 업무 화면은 하루 종일 보는 화면이라
  정보 밀도가 중요하지만, 고객 화면은 가끔 들어와 확인하는 화면이다.
  고객 화면에서는 `text-2xs`와 `0.5625rem`을 쓰지 않는다.
- 고객 화면의 각 영역은 **카드 밖 섹션 머리말**(색 아이콘 + 큰 제목 + 한 줄 설명)로 구분한다.
  색은 영역 성격에 맞춘다 — 현황 pine / 일정 sand / 답변·확장 clay / 내 요청 mist.

## 8. Demo / Live 원칙

화면의 값은 세 종류로만 구분해서 표시한다.

| 구분 | 의미 | 표기 |
| --- | --- | --- |
| DEMO | 시연용 가상 데이터 | `DEMO DATA` 칩 |
| TARGET | 달성하려는 목표치 | `TARGET` 칩 |
| ACTUAL | 실제 기록된 값 | `ACTUAL` 칩 |

지켜야 할 것:

- 실제 근거가 없는 수치를 **실적처럼** 표시하지 않는다.
- 정책자금 **선정 확률·적합도 %** 를 쓰지 않는다. 상태값(우선 검토 / 조건 확인 필요 / 중장기 검토 / 현재 대상 아님)만 쓴다.
- 실측값이 없는 개선 지표는 "측정 준비 중" 으로 둔다. 임의 개선율을 넣지 않는다.
- LLM이 연결되지 않은 기능은 `AI PREVIEW`(규칙 기반)로 명시한다.
- 사진은 실제 제공 전까지 자리표시자만 둔다. Stock/AI 이미지를 넣지 않는다.
