# DECISIONS — 주요 결정 기록

각 항목은 **무엇을 정했는지**와 **왜 그렇게 했는지**를 함께 남긴다.

---

## D-01. 기존 Business AX를 유지하고 확장한다

전면 재작성하지 않는다. 기존 라우트·디자인·데이터 구조를 그대로 두고 위에 얹는다.

이유: 이미 대표자 시연이 가능한 수준의 화면이 있고, 그것을 갈아엎는 것은
시간 낭비이자 위험이다. URL도 유지해 기존 공유 링크가 깨지지 않게 한다.

## D-02. 고객 플랫폼을 별도 Surface로 추가한다

`/customer/*`는 Business AX의 사이드바를 복제하지 않고 자체 셸(`CustomerShell`)을 쓴다.
Provider(설정·store·토스트)는 공유한다.

이유: 고객에게 필요한 정보량과 직원에게 필요한 정보량이 다르다. 같은 크롬을 쓰면
고객 화면이 관리자 화면처럼 무거워진다. 반면 데이터는 같아야 Closed Loop가 성립한다.

## D-03. 고객 플랫폼은 쇼핑몰이 아니라 장비 운영지원 포털이다

상품 목록·장바구니·결제를 만들지 않는다. 대신 **보유 장비 → 교정 시점 → 요청**의 흐름만 만든다.

이유: 제이랩테크의 사업 전환 방향은 "더 많이 파는 것"이 아니라
"납품한 장비의 운영을 계속 함께 관리하는 것"이다. 화면 구조가 그 메시지를 담아야 한다.

## D-04. Closed Loop는 2개만, 대신 실제로 동작하게 만든다

교정 요청 / 소모품·추가 계측. 둘 다 고객 → 내부 → 고객으로 상태가 실제로 왕복한다.

이유: 화면만 예쁜 데모는 설득력이 없다. 개수를 늘리는 것보다 하나라도
localStorage에 저장되고 양쪽 화면에 반영되는 것이 중요하다.

## D-05. Demo Data와 Actual을 화면에서 분리한다

`DEMO DATA` / `TARGET` / `ACTUAL` 세 가지 칩으로 구분하고, 탑바에 상태 팝오버를 둔다.

이유: 재고·고객 수치는 시연용이지만 발주·견적·요청 기록은 진짜다.
이 둘을 섞어 보여주면 대표자가 판단을 잘못하게 되고, 심사에서도 신뢰를 잃는다.

## D-06. 데이터 규모를 실제 회사 규모에 맞춘다

총 재고 2억 2,530만원, 월 출고 3,000~4,500만원 대로 재조정했다.
기존의 48.7억 재고 / 62.3억 수요는 제거했다.

이유: 매출 4.6억 회사에 재고 48억은 즉시 거짓임이 드러난다.
데모의 신뢰는 화려함이 아니라 그럴듯함에서 온다.

## D-07. 정책자금은 확률이 아니라 상태로 표시한다

`적합도 88%` 같은 표현을 제거하고
`우선 검토 / 조건 확인 필요 / 중장기 검토 / 현재 대상 아님`으로 바꿨다.

이유: 선정 확률은 누구도 계산할 수 없다. 숫자로 쓰면 근거를 요구받고 무너진다.
"지금 어느 단계에서 검토할 수 있는가"는 플랫폼 데이터로 실제 말할 수 있다.

## D-08. 실측이 없는 개선 지표는 "측정 준비 중"으로 둔다

Before/After를 [도입 전 / 목표 / 실측] 3단으로 나누고, 실측이 없으면 비운다.

이유: 임의의 개선율을 성과처럼 적는 순간 그 문서 전체의 신뢰가 떨어진다.
"측정 구조는 있고 아직 데이터가 없다"가 훨씬 강한 진술이다.

## D-09. 규칙 기반 기능을 AI가 생성했다고 표현하지 않는다

AI 추천·견적과 산업계측 설계에 `AI PREVIEW` / `AX DESIGN PREVIEW` 칩을 붙였다.

이유: 실제 LLM API가 연결되어 있지 않다. 연결된 것처럼 보이게 하면
기술 실사에서 즉시 문제가 된다. 대신 "연결하면 그대로 대체된다"는 구조를 보여준다.

## D-10. 3단계는 중장기 검토로만 표현한다

원격 계측·데이터 리포트·예지보전·정기관리 계약은 클릭 불가 목차로만 둔다.

이유: 없는 기능을 있는 것처럼 보이면 안 된다. 다만 방향은 보여야
"이 회사가 어디로 가는가"가 읽힌다. 배지로 구분하는 것이 정직하면서도 효과적이다.

## D-11. 사진은 자리표시자만 두고 임의 이미지를 넣지 않는다

기획의도에 16:9 비율 고정 자리표시자 3개(Current Problem / Improved Workflow / Growth).

이유: Stock Photo나 AI 생성 이미지는 실제 현장이 아니다.
나중에 진짜 사진이 들어와도 레이아웃이 흔들리지 않도록 비율만 미리 잡아 둔다.

## D-12. 테마는 CSS 변수로만 바꾼다

Tailwind 색상 이름(pine/ivory/sand/sage)은 그대로 두고 값만 `var(--*)`로 바꿨다.
테마 6종은 `[data-theme]` 블록에서 변수만 재정의한다.

이유: 컴포넌트의 className을 하나도 건드리지 않고 테마를 바꿀 수 있다.
전면 재설계 없이 요구사항을 만족하는 유일하게 안전한 방법이었다.

## D-13. 업무 카드는 순백으로 올린다

`--ivory-50`을 `#FDFCF8` → `#FFFFFF`로 바꿨다. 캔버스는 아이보리 그대로.

이유: 모든 카드가 아이보리 계열이라 화면이 뿌옇게 보였다.
토큰 한 줄로 데이터 표면의 대비만 올렸고, 브랜드 정체성은 유지된다.

## D-14. 반복 애니메이션을 제거한다

무한 펄스 → 진입 시 1회(`attention-once`), 기획의도 shimmer → 3회 후 정지.
`data-motion="reduced"`와 `prefers-reduced-motion`을 모두 존중한다.

이유: 계속 움직이는 요소는 업무 화면에서 피로하고 저렴해 보인다.
빠른 hover 반응(160~180ms)이 고급스러움에 더 기여한다.

## D-15. Device Preview는 재귀를 구조적으로 막는다

iframe URL에 `?preview=1`을 붙이고, 그 파라미터가 있으면 버튼 자체를 렌더하지 않는다.

이유: 상태 플래그로 막으면 새로고침·직접 URL 진입에서 뚫린다.
URL에 표시하면 어떤 경로로 들어와도 재귀가 생기지 않는다.

## D-16. 탑바 요소는 화면 너비가 아니라 남는 폭을 기준으로 노출한다

시계·라벨의 노출 지점을 Tailwind 기본 단계(`lg` 1024 / `xl` 1280)에서
실측 기준(`min-[1100px]` / `min-[1180px]` / `min-[1500px]` / `min-[1620px]`)으로 옮겼다.

이유: 1024px부터 사이드바가 272px를 가져가므로 탑바에 남는 폭은 화면 너비와 다르다.
기본 breakpoint를 그대로 쓰면 1024px에서 23px, 1280px에서 109px씩 가로 스크롤이 생겼다.
글자 크기 "크게"에서는 rem 기반 폭이 더 커지므로 그 조건까지 통과하는 값으로 잡았다.

## D-17. 확장 사업영역을 별도 문서가 아니라 고객 화면 안에 둔다

`/customer/services`를 만들어 이용 가능 3종 / 준비 중 4종 / 검토 중 4종을
같은 카드 구조로 늘어놓고, 클릭하면 상세가 열리게 했다.

이유: 확장 계획을 제안서에만 적으면 "말로만 하는 계획"이 된다. 고객이 실제로 쓰는
화면에서 "지금 되는 것" 바로 옆에 "준비 중인 것"이 같은 형식으로 놓이면,
이 회사가 어디까지 가려는지가 설명 없이 읽힌다. 심사에서도 같은 화면을 그대로 보여줄 수 있다.

## D-18. 확장 항목에 예상 매출액을 쓰지 않는다

각 서비스에 [지금 준비된 것 / 어떻게 구현되는가 / 고객이 얻는 것 / 수익 구조 /
선행 조건] 다섯 칸을 두되, 수익은 금액이 아니라 **구조**로만 적는다.
"장비 1대당 연간 관리비", "일·주 단위 임대료" 까지만 쓰고 숫자를 넣지 않았다.

이유: 근거 없는 매출 추정은 한 번만 따져 물으면 무너진다. 대신 "건별 매출이
반복 매출로 바뀐다"는 구조 변화는 지금 있는 데이터로 설명할 수 있다.
적용 대상 규모도 지어내지 않고 등록된 장비 수에서 센다.

## D-19. 선행 조건을 각 항목마다 함께 적는다

모든 확장 항목에 "시작하기 전에 정리되어야 하는 것"을 넣었다.
연간 계약에는 "감당하지 못하는 계약은 매출이 아니라 부채가 된다",
예지보전에는 "정확도가 확보되기 전에 알림을 보내면 신뢰를 잃는다"까지 적었다.

이유: 장점만 나열된 확장 계획은 검토를 안 해봤다는 뜻으로 읽힌다.
못 하는 이유를 같이 아는 쪽이 더 준비된 회사로 보인다.

## D-20. 화면 전환은 버튼 하나가 아니라 두 칸짜리 스위처로 만든다

각 화면에 "고객 플랫폼" / "관리자 화면" 버튼이 하나씩 따로 있던 것을,
[관리자 화면 | 고객 화면] 두 칸을 항상 함께 보여주고 현재 있는 쪽을 채워
표시하는 `SurfaceSwitcher` 하나로 합쳤다.

이유: 버튼 하나만 있으면 그 라벨이 "지금 있는 곳"인지 "갈 곳"인지 알 수 없다.
두 칸을 같이 두면 현재 위치와 이동 방향이 설명 없이 읽힌다.
`aria-current="page"`로 스크린리더에도 같은 정보를 준다.

## D-21. 고객 플랫폼에도 햄버거 드로어를 두되, 메뉴와 목차를 같이 넣는다

`CustomerDrawer`는 [계정 / 메뉴 4개 / **이 페이지 목차** / 화면 전환] 순서다.
목차는 페이지마다 다르게 만든다 — 홈은 섹션 7개, 내 장비는 장비 5대,
요청 내역은 요청 건별, 서비스는 3단계 그룹.

이유: 페이지가 길어져 원하는 곳을 찾으려면 스크롤을 계속 내려야 했다.
메뉴만 있는 드로어는 그 문제를 풀지 못한다. 페이지 안 위치까지 짚어 줘야
"목차"가 된다. 이동은 `scrollIntoView`로 하고 앵커에 `scroll-mt-20`을 줘
고정 헤더에 가리지 않게 했다.

## D-22. 보조 강조색은 clay·mist 두 가지만, 역시 CSS 변수로 넣는다

`--clay-*`(따뜻한 테라코타)와 `--mist-*`(차가운 슬레이트블루)를 테마 6종 전부에
정의했다. 사이드바 아이콘 팔레트에 이미 있던 clay·steel 톤과 같은 계열이다.

이유: 딥그린·아이보리·골드 3색만으로는 서비스 3단계처럼 "같은 위계의 서로 다른
상태"를 구분하기 어려웠다. 색을 늘리되 두 개까지만, 그리고 반드시 변수로 넣어
테마를 바꿔도 조화가 유지되게 했다. 쓰는 곳도 제한한다 —
준비 중(clay) / 검토 중(mist), 소모품 일정(clay·mist), 알림 배지(clay).

## D-23. 탑바 라벨 노출을 다섯 곳에서 두 곳으로 줄인다

시계·스위처·모바일 보기·사용 방법·기획의도 다섯 개가 각자 다른 폭에서 라벨을
켜고 있었다. 모바일 보기와 사용 방법은 라벨을 없애고 아이콘만 남겼다
(둘 다 사이드바 목차에 같은 항목이 있다).

이유: 라벨이 켜지는 지점이 많을수록 어느 하나만 넓어져도 탑바가 밀린다.
실제로 스위처를 두 칸으로 바꾸자 글자 크기 "크게"에서 네 구간이 한꺼번에
넘쳤다. 경쟁하는 요소를 줄이는 것이 임계값을 정교하게 맞추는 것보다 안전하다.

## D-24. 고객 셸의 최대 폭을 max-w-6xl로 넓힌다

컨테이너가 `max-w-5xl`(약 1,242px)에서 멈춰 있어, 화면이 1440px이 되어도
헤더에 자리가 늘지 않고 브랜드명만 잘렸다. 좁은 폭에서는 브랜드명을
"제이랩테크"로 줄여 아예 잘리지 않게 했다.

이유: 노출 임계값은 화면 너비 기준인데 실제 여유 폭은 컨테이너 기준이라
둘이 어긋나 있었다. 컨테이너를 넓히면서 고객 화면 자체도 덜 답답해졌다.
